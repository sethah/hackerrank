object AlgoQuestions {

  def main(args: Array[String]): Unit = {
    lp("racecarasdf")
  }

  def lp(s: String): Unit = {
    val memo = Array.fill(s.length)(new Array[Int](s.length))
    val chars = s.toArray

  }

}
